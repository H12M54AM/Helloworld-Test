#include <iostream>

int main () {
    /* std::cout is what will output your code to the terminal, "cout" is the output
    while "cin" is the input, and that is where the user will type somehting into that input.*/
    
    std::cout << "Hello world";

    return 0;
}

void gamer () {
    
}